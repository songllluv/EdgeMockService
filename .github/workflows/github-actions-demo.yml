name: Keep Supabase Alive
于:
 schedule:
   - cron: '0 8 * * *'
jobs:
 ping:
   runs-on: ubuntu-latest
   steps:
     - name: Ping Supabase
       run: curl -s https://supabase.com/dashboard/api/edge-functions/
