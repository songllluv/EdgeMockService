name: Keep Supabase Alive
on:
 schedule:
   - cron: '0 8 * * *'
jobs:
 ping:
   runs-on: ubuntu-latest
   steps:
     - name: Ping Supabase
       run: curl -s https://ljrfkrxwkqnruheucstl.supabase.co/functions/v1/songlllEdgeService
